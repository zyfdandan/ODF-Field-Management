[
  {
    "id": "odf_field_tab",
    "type": "tab",
    "label": "ODF 现场登记",
    "disabled": false,
    "info": "ODF 浏览管理 + 扫码登记，转发到 field_api.py"
  },
  {
    "id": "odf_init",
    "type": "inject",
    "z": "odf_field_tab",
    "name": "启动时加载配置",
    "props": [{"p": "payload"}],
    "repeat": "",
    "crontab": "",
    "once": true,
    "onceDelay": 0.5,
    "topic": "",
    "payload": "{\"field_api_url\":\"http://127.0.0.1:8765\"}",
    "payloadType": "json",
    "x": 180,
    "y": 40,
    "wires": [["odf_set_cfg"]]
  },
  {
    "id": "odf_set_cfg",
    "type": "function",
    "z": "odf_field_tab",
    "name": "global.fieldPortal",
    "func": "global.set('fieldPortal', msg.payload);\nreturn msg;",
    "outputs": 1,
    "x": 400,
    "y": 40,
    "wires": [[]]
  },
  {
    "id": "odf_http_root",
    "type": "http in",
    "z": "odf_field_tab",
    "name": "GET / 首页",
    "url": "/",
    "method": "get",
    "upload": false,
    "x": 140,
    "y": 100,
    "wires": [["odf_proxy_portal"]]
  },
  {
    "id": "odf_http_portal",
    "type": "http in",
    "z": "odf_field_tab",
    "name": "GET /portal",
    "url": "/portal",
    "method": "get",
    "upload": false,
    "x": 150,
    "y": 140,
    "wires": [["odf_proxy_portal"]]
  },
  {
    "id": "odf_proxy_portal",
    "type": "function",
    "z": "odf_field_tab",
    "name": "转发浏览页",
    "func": "const cfg = global.get('fieldPortal') || flow.get('fieldPortal') || {};\nconst base = cfg.field_api_url || 'http://127.0.0.1:8765';\nconst req = msg.req || {};\nconst h = req.headers || {};\nlet ip = String((h['x-forwarded-for'] || '').split(',')[0].trim() || h['x-real-ip'] || req.ip || (req.connection && req.connection.remoteAddress) || '').replace(/^::ffff:/, '');\nmsg.url = base + '/portal';\nmsg.method = 'GET';\nmsg.headers = Object.assign({}, msg.headers || {}, {\n  'X-Forwarded-For': ip || 'unknown',\n  'X-Real-IP': ip || 'unknown',\n  'User-Agent': h['user-agent'] || '',\n  'Accept-Language': h['accept-language'] || ''\n});\nreturn msg;",
    "outputs": 1,
    "x": 340,
    "y": 120,
    "wires": [["odf_http_req"]]
  },
  {
    "id": "odf_http_trunk",
    "type": "http in",
    "z": "odf_field_tab",
    "name": "GET /trunk 新增缆段",
    "url": "/trunk",
    "method": "get",
    "upload": false,
    "x": 160,
    "y": 240,
    "wires": [["odf_proxy_trunk"]]
  },
  {
    "id": "odf_proxy_trunk",
    "type": "function",
    "z": "odf_field_tab",
    "name": "转发新增缆段页",
    "func": "const cfg = global.get('fieldPortal') || flow.get('fieldPortal') || {};\nconst base = cfg.field_api_url || 'http://127.0.0.1:8765';\nconst req = msg.req || {};\nconst h = req.headers || {};\nlet ip = String((h['x-forwarded-for'] || '').split(',')[0].trim() || h['x-real-ip'] || req.ip || (req.connection && req.connection.remoteAddress) || '').replace(/^::ffff:/, '');\nmsg.url = base + '/trunk';\nmsg.method = 'GET';\nmsg.headers = Object.assign({}, msg.headers || {}, {\n  'X-Forwarded-For': ip || 'unknown',\n  'X-Real-IP': ip || 'unknown',\n  'User-Agent': h['user-agent'] || '',\n  'Accept-Language': h['accept-language'] || ''\n});\nreturn msg;",
    "outputs": 1,
    "x": 360,
    "y": 240,
    "wires": [["odf_http_req"]]
  },
  {
    "id": "odf_http_form",
    "type": "http in",
    "z": "odf_field_tab",
    "name": "GET /odf 表单",
    "url": "/odf",
    "method": "get",
    "upload": false,
    "x": 150,
    "y": 200,
    "wires": [["odf_proxy_form"]]
  },
  {
    "id": "odf_proxy_form",
    "type": "function",
    "z": "odf_field_tab",
    "name": "转发表单",
    "func": "const cfg = global.get('fieldPortal') || flow.get('fieldPortal') || {};\nconst base = cfg.field_api_url || 'http://127.0.0.1:8765';\nconst odf = msg.req.query.odf || msg.req.query.d || '';\nconst req = msg.req || {};\nconst h = req.headers || {};\nlet ip = String((h['x-forwarded-for'] || '').split(',')[0].trim() || h['x-real-ip'] || req.ip || (req.connection && req.connection.remoteAddress) || '').replace(/^::ffff:/, '');\nmsg.url = base + '/form?odf=' + encodeURIComponent(odf);\nmsg.method = 'GET';\nmsg.headers = Object.assign({}, msg.headers || {}, {\n  'X-Forwarded-For': ip || 'unknown',\n  'X-Real-IP': ip || 'unknown',\n  'User-Agent': h['user-agent'] || '',\n  'Accept-Language': h['accept-language'] || ''\n});\nreturn msg;",
    "outputs": 1,
    "x": 340,
    "y": 200,
    "wires": [["odf_http_req"]]
  },
  {
    "id": "odf_http_api_get",
    "type": "http in",
    "z": "odf_field_tab",
    "name": "GET /api/*",
    "url": "/api/:path(*)",
    "method": "get",
    "upload": false,
    "x": 150,
    "y": 280,
    "wires": [["odf_proxy_api_root"]]
  },
  {
    "id": "odf_http_odf_api_get",
    "type": "http in",
    "z": "odf_field_tab",
    "name": "GET /odf/api/*",
    "url": "/odf/api/:path(*)",
    "method": "get",
    "upload": false,
    "x": 160,
    "y": 320,
    "wires": [["odf_proxy_api_odf"]]
  },
  {
    "id": "odf_proxy_api_root",
    "type": "function",
    "z": "odf_field_tab",
    "name": "转发 /api GET",
    "func": "const cfg = global.get('fieldPortal') || flow.get('fieldPortal') || {};\nconst base = cfg.field_api_url || 'http://127.0.0.1:8765';\nconst p = msg.req.params.path || 'tree';\nconst q = msg.req.query || {};\nconst qs = Object.keys(q).map(k => k + '=' + encodeURIComponent(q[k])).join('&');\nconst req = msg.req || {};\nconst h = req.headers || {};\nlet ip = String((h['x-forwarded-for'] || '').split(',')[0].trim() || h['x-real-ip'] || req.ip || (req.connection && req.connection.remoteAddress) || '').replace(/^::ffff:/, '');\nmsg.url = base + '/api/' + p + (qs ? '?' + qs : '');\nmsg.method = 'GET';\nmsg.headers = Object.assign({}, msg.headers || {}, {\n  'X-Forwarded-For': ip || 'unknown',\n  'X-Real-IP': ip || 'unknown',\n  'User-Agent': h['user-agent'] || '',\n  'Accept-Language': h['accept-language'] || '',\n  'Cache-Control': 'no-cache'\n});\nreturn msg;",
    "outputs": 1,
    "x": 360,
    "y": 280,
    "wires": [["odf_http_req"]]
  },
  {
    "id": "odf_proxy_api_odf",
    "type": "function",
    "z": "odf_field_tab",
    "name": "转发 /odf/api GET",
    "func": "const cfg = global.get('fieldPortal') || flow.get('fieldPortal') || {};\nconst base = cfg.field_api_url || 'http://127.0.0.1:8765';\nconst p = msg.req.params.path || 'context';\nconst q = msg.req.query || {};\nconst qs = Object.keys(q).map(k => k + '=' + encodeURIComponent(q[k])).join('&');\nconst req = msg.req || {};\nconst h = req.headers || {};\nlet ip = String((h['x-forwarded-for'] || '').split(',')[0].trim() || h['x-real-ip'] || req.ip || (req.connection && req.connection.remoteAddress) || '').replace(/^::ffff:/, '');\nmsg.url = base + '/api/' + p + (qs ? '?' + qs : '');\nmsg.method = 'GET';\nmsg.headers = Object.assign({}, msg.headers || {}, {\n  'X-Forwarded-For': ip || 'unknown',\n  'X-Real-IP': ip || 'unknown',\n  'User-Agent': h['user-agent'] || '',\n  'Accept-Language': h['accept-language'] || '',\n  'Cache-Control': 'no-cache'\n});\nreturn msg;",
    "outputs": 1,
    "x": 370,
    "y": 320,
    "wires": [["odf_http_req"]]
  },
  {
    "id": "odf_http_api_post",
    "type": "http in",
    "z": "odf_field_tab",
    "name": "POST /api/submit",
    "url": "/api/submit",
    "method": "post",
    "upload": false,
    "x": 170,
    "y": 400,
    "wires": [["odf_proxy_submit_root"]]
  },
  {
    "id": "odf_http_odf_api_post",
    "type": "http in",
    "z": "odf_field_tab",
    "name": "POST /odf/api/submit",
    "url": "/odf/api/submit",
    "method": "post",
    "upload": false,
    "x": 180,
    "y": 440,
    "wires": [["odf_proxy_submit_odf"]]
  },
  {
    "id": "odf_proxy_submit_root",
    "type": "function",
    "z": "odf_field_tab",
    "name": "转发 /api POST",
    "func": "const cfg = global.get('fieldPortal') || flow.get('fieldPortal') || {};\nconst base = cfg.field_api_url || 'http://127.0.0.1:8765';\nconst req = msg.req || {};\nconst h = req.headers || {};\nlet ip = String((h['x-forwarded-for'] || '').split(',')[0].trim() || h['x-real-ip'] || req.ip || (req.connection && req.connection.remoteAddress) || '').replace(/^::ffff:/, '');\nmsg.url = base + '/api/submit';\nmsg.method = 'POST';\nmsg.headers = {\n  'Content-Type': 'application/json',\n  'X-Forwarded-For': ip || 'unknown',\n  'X-Real-IP': ip || 'unknown',\n  'User-Agent': h['user-agent'] || '',\n  'Accept-Language': h['accept-language'] || ''\n};\nmsg.payload = msg.payload;\nreturn msg;",
    "outputs": 1,
    "x": 370,
    "y": 400,
    "wires": [["odf_http_req"]]
  },
  {
    "id": "odf_proxy_submit_odf",
    "type": "function",
    "z": "odf_field_tab",
    "name": "转发 /odf/api POST",
    "func": "const cfg = global.get('fieldPortal') || flow.get('fieldPortal') || {};\nconst base = cfg.field_api_url || 'http://127.0.0.1:8765';\nconst req = msg.req || {};\nconst h = req.headers || {};\nlet ip = String((h['x-forwarded-for'] || '').split(',')[0].trim() || h['x-real-ip'] || req.ip || (req.connection && req.connection.remoteAddress) || '').replace(/^::ffff:/, '');\nmsg.url = base + '/api/submit';\nmsg.method = 'POST';\nmsg.headers = {\n  'Content-Type': 'application/json',\n  'X-Forwarded-For': ip || 'unknown',\n  'X-Real-IP': ip || 'unknown',\n  'User-Agent': h['user-agent'] || '',\n  'Accept-Language': h['accept-language'] || ''\n};\nmsg.payload = msg.payload;\nreturn msg;",
    "outputs": 1,
    "x": 380,
    "y": 440,
    "wires": [["odf_http_req"]]
  },
  {
    "id": "odf_http_room_order",
    "type": "http in",
    "z": "odf_field_tab",
    "name": "POST /api/room-order",
    "url": "/api/room-order",
    "method": "post",
    "upload": false,
    "x": 190,
    "y": 520,
    "wires": [["odf_proxy_room_order"]]
  },
  {
    "id": "odf_proxy_room_order",
    "type": "function",
    "z": "odf_field_tab",
    "name": "转发 /api room-order",
    "func": "const cfg = global.get('fieldPortal') || flow.get('fieldPortal') || {};\nconst base = cfg.field_api_url || 'http://127.0.0.1:8765';\nconst req = msg.req || {};\nconst h = req.headers || {};\nlet ip = String((h['x-forwarded-for'] || '').split(',')[0].trim() || h['x-real-ip'] || req.ip || (req.connection && req.connection.remoteAddress) || '').replace(/^::ffff:/, '');\nmsg.url = base + '/api/room-order';\nmsg.method = 'POST';\nmsg.headers = {\n  'Content-Type': 'application/json',\n  'X-Forwarded-For': ip || 'unknown',\n  'X-Real-IP': ip || 'unknown',\n  'User-Agent': h['user-agent'] || '',\n  'Accept-Language': h['accept-language'] || ''\n};\nmsg.payload = msg.payload;\nreturn msg;",
    "outputs": 1,
    "x": 400,
    "y": 520,
    "wires": [["odf_http_req"]]
  },
  {
    "id": "odf_http_beacon",
    "type": "http in",
    "z": "odf_field_tab",
    "name": "POST /api/audit/beacon",
    "url": "/api/audit/beacon",
    "method": "post",
    "upload": false,
    "x": 200,
    "y": 580,
    "wires": [["odf_proxy_beacon"]]
  },
  {
    "id": "odf_http_beacon_odf",
    "type": "http in",
    "z": "odf_field_tab",
    "name": "POST /odf/api/audit/beacon",
    "url": "/odf/api/audit/beacon",
    "method": "post",
    "upload": false,
    "x": 210,
    "y": 620,
    "wires": [["odf_proxy_beacon"]]
  },
  {
    "id": "odf_proxy_beacon",
    "type": "function",
    "z": "odf_field_tab",
    "name": "转发 audit beacon",
    "func": "const cfg = global.get('fieldPortal') || flow.get('fieldPortal') || {};\nconst base = cfg.field_api_url || 'http://127.0.0.1:8765';\nconst req = msg.req || {};\nconst h = req.headers || {};\nlet ip = String((h['x-forwarded-for'] || '').split(',')[0].trim() || h['x-real-ip'] || req.ip || (req.connection && req.connection.remoteAddress) || '').replace(/^::ffff:/, '');\nmsg.url = base + '/api/audit/beacon';\nmsg.method = 'POST';\nmsg.headers = {\n  'Content-Type': 'application/json',\n  'X-Forwarded-For': ip || 'unknown',\n  'X-Real-IP': ip || 'unknown',\n  'User-Agent': h['user-agent'] || '',\n  'Accept-Language': h['accept-language'] || ''\n};\nmsg.payload = msg.payload;\nreturn msg;",
    "outputs": 1,
    "x": 410,
    "y": 600,
    "wires": [["odf_http_req"]]
  },
  {
    "id": "odf_http_req",
    "type": "http request",
    "z": "odf_field_tab",
    "name": "Field API",
    "method": "use",
    "ret": "obj",
    "paytoqs": "ignore",
    "url": "",
    "tls": "",
    "persist": false,
    "proxy": "",
    "authType": "",
    "x": 560,
    "y": 280,
    "wires": [["odf_http_res"]]
  },
  {
    "id": "odf_http_res",
    "type": "http response",
    "z": "odf_field_tab",
    "name": "响应",
    "statusCode": "",
    "headers": {},
    "x": 740,
    "y": 280,
    "wires": []
  }
]
